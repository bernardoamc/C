It's possible to have more than one entry in the /etc/passwd file with the
same UID. With this we can have multiple login names for the same UID and
also have multiple users accessing the same resources using different passwords.
